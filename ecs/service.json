{
  "serviceName": "shopsmart-backend",
  "cluster": "{{ecs-cluster}}",
  "taskDefinition": "shopsmart-backend",
  "desiredCount": 1,
  "launchType": "FARGATE",
  "networkConfiguration": {
    "awsvpcConfiguration": {
      "subnets": ["{{subnet-id}}"],
      "securityGroups": ["{{security-group-id}}"],
      "assignPublicIp": "ENABLED"
    }
  },
  "loadBalancers": [
    {
      "targetGroupArn": "{{target-group-arn}}",
      "containerName": "shopsmart-backend",
      "containerPort": 5001
    }
  ],
  "deploymentConfiguration": {
    "maximumPercent": 200,
    "minimumHealthyPercent": 100
  }
}